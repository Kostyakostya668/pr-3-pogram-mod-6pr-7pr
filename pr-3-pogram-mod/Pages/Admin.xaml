<Page x:Class="pr_3_pogram_mod.Pages.Admin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr_3_pogram_mod.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="800"
      Title="Admin"
      Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>-->

        <TextBlock x:Name="textName" Grid.Row="0" Margin="10" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Резиденты" />
            <TextBlock Text="Поиск по имени:"/>
            <TextBox x:Name="tbNameFind" Width="150" Margin="0,5,10,20" TextChanged="tbNameFind_TextChanged"/>
            <Button x:Name="brRes" Content="Обновить" Click="brRes_Click"/>
        </StackPanel>

        <ListView Name="lvRes" Grid.Row="2" Margin="0,0,0,0" MouseDoubleClick="lvRes_MouseDoubleClick" 
                  d:ItemsSource="{d:SampleData ItemCount = 1 }" HorizontalAlignment="Stretch">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50"/>
                                <RowDefinition Height="60"/>
                            </Grid.RowDefinitions>

                            <Image Grid.Row="0" Margin="5" Source="/Resources/iconUser.png"/>

                            <StackPanel Grid.Row="1">
                                <TextBlock Text="{Binding name}"/>
                                <TextBlock Text="{Binding surname}"/>
                            </StackPanel>
                            
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
