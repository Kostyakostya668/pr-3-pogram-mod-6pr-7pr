<Page x:Class="pr_3_pogram_mod.Pages.Admin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr_3_pogram_mod.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="800"
      Title="Admin"
      Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="textName" Grid.Row="0" Margin="10" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Резиденты" Margin="5,0"/>
            <TextBlock Text="Поиск:" Margin="5,0"/>
            <TextBox x:Name="tbNameFind" Width="150" Margin="0,5,10,5" TextChanged="tbNameFind_TextChanged"/>
            <ComboBox Name="chosenSort" SelectionChanged="chosenSort_SelectionChanged" Margin="5,0">
                <ComboBoxItem>Просто</ComboBoxItem>
                <ComboBoxItem>Только имя</ComboBoxItem>
                <ComboBoxItem>Только фамилия</ComboBoxItem>
            </ComboBox>
            <Button x:Name="brRes" Content="Обновить" Click="brRes_Click" Margin="5,0"/>
            <Button x:Name="btAddRes" Content="Добавить" Click="btAddRes_Click" Margin="5,0"/>
        </StackPanel>

        <ListView Name="lvRes" Grid.Row="2" Margin="10" MouseDoubleClick="lvRes_MouseDoubleClick" 
                  HorizontalAlignment="Stretch">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="5"/>
                </Style>
            </ListView.ItemContainerStyle>

        </ListView>
    </Grid>
</Page>